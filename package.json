{
  "name": "youtube-spoiler-shield",
  "version": "0.1.0",
  "private": true,
  "description": "A privacy-first Chrome extension that blurs YouTube spoilers based on local keywords.",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node scripts/test-matching.js",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
