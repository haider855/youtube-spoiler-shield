namespace SpoilerShieldContent {
  export function blockCard(_card: HTMLElement): void {
    // Blocking renderer will be implemented in a later phase.
  }

  export function unblockCard(_card: HTMLElement): void {
    // Unblocking renderer will be implemented in a later phase.
  }
}
